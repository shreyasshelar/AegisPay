# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — use values-dev.yaml instead
#
# This file is kept only for reference. The "on-prem" environment has been
# renamed to "dev" to match the canonical three-environment model:
#   Local → Dev (k3s, cost-optimised) → Prod (AWS EKS)
#
# Migrate: replace values-onprem.yaml with values-dev.yaml in all Helm commands.
# ─────────────────────────────────────────────────────────────────────────────
