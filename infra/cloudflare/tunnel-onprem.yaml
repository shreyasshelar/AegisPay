# ─────────────────────────────────────────────────────────────────────────────
# DEPRECATED — use tunnel-dev.yaml instead
#
# The "on-prem" environment has been renamed to "dev" to match the canonical
# three-environment model: Local → Dev (k3s, cost-optimised) → Prod (AWS EKS)
#
# Migrate:
#   kubectl apply -f infra/cloudflare/tunnel-dev.yaml
# ─────────────────────────────────────────────────────────────────────────────
