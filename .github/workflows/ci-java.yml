# ─────────────────────────────────────────────────────────────────────────────
# AegisPay — Smart Java CI
#
# Change-detection strategy:
#   • Runs on direct push to main OR dev only (no PR triggers)
#   • dorny/paths-filter detects WHICH services changed
#   • Each service job only runs if its own files changed, OR if shared libs
#     changed (because every service imports common-*)
#   • Docker images are built & pushed to GHCR only on push to main
#     (prod branch); dev branch skips Docker push to save registry costs
#   • data-pipeline and reconciliation-service included as Phase 11 services
# ─────────────────────────────────────────────────────────────────────────────
name: CI — Java (Smart)

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'libs/**'
      - 'services/**'
      - 'pom.xml'
      - '.github/workflows/ci-java.yml'

env:
  JAVA_VERSION: "21"
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/aegispay

# ─────────────────────────────────────────────────────────────────────────────
jobs:

  # ── Step 0: Detect which services actually changed ──────────────────────────
  detect-changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest
    outputs:
      libs:                   ${{ steps.filter.outputs.libs }}
      api-gateway:            ${{ steps.filter.outputs.api-gateway }}
      user-service:           ${{ steps.filter.outputs.user-service }}
      transaction-service:    ${{ steps.filter.outputs.transaction-service }}
      ledger-service:         ${{ steps.filter.outputs.ledger-service }}
      payment-orchestrator:   ${{ steps.filter.outputs.payment-orchestrator }}
      risk-engine:            ${{ steps.filter.outputs.risk-engine }}
      notification-service:   ${{ steps.filter.outputs.notification-service }}
      ai-platform:            ${{ steps.filter.outputs.ai-platform }}
      reconciliation-service: ${{ steps.filter.outputs.reconciliation-service }}
      data-pipeline:          ${{ steps.filter.outputs.data-pipeline }}
      helm:                   ${{ steps.filter.outputs.helm }}
    steps:
      - uses: actions/checkout@v4
        with:
          # fetch-depth 0 gives full history so dorny can compare base refs on PRs
          fetch-depth: 0

      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            libs:
              - 'libs/**'
              - 'pom.xml'
            api-gateway:
              - 'services/api-gateway/**'
            user-service:
              - 'services/user-service/**'
            transaction-service:
              - 'services/transaction-service/**'
            ledger-service:
              - 'services/ledger-service/**'
            payment-orchestrator:
              - 'services/payment-orchestrator/**'
            risk-engine:
              - 'services/risk-engine/**'
            notification-service:
              - 'services/notification-service/**'
            ai-platform:
              - 'services/ai-platform/**'
            reconciliation-service:
              - 'services/reconciliation-service/**'
            data-pipeline:
              - 'services/data-pipeline/**'
            helm:
              - 'infra/helm/**'

  # ── Step 1: Build shared libs (needed by all services) ─────────────────────
  # Runs whenever ANY java code changed (libs or any service)
  build-libs:
    name: Build Shared Libs
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.libs == 'true' ||
      needs.detect-changes.outputs['api-gateway'] == 'true' ||
      needs.detect-changes.outputs['user-service'] == 'true' ||
      needs.detect-changes.outputs['transaction-service'] == 'true' ||
      needs.detect-changes.outputs['ledger-service'] == 'true' ||
      needs.detect-changes.outputs['payment-orchestrator'] == 'true' ||
      needs.detect-changes.outputs['risk-engine'] == 'true' ||
      needs.detect-changes.outputs['notification-service'] == 'true' ||
      needs.detect-changes.outputs['ai-platform'] == 'true' ||
      needs.detect-changes.outputs['reconciliation-service'] == 'true' ||
      needs.detect-changes.outputs['data-pipeline'] == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: temurin
          cache: maven

      - name: Build & install shared libs
        run: |
          mvn --batch-mode --no-transfer-progress clean install \
            -pl libs/common-domain,libs/common-security,libs/common-kafka,libs/common-observability \
            -DskipTests

      - name: Cache libs into Maven local repo
        uses: actions/cache/save@v4
        with:
          path: ~/.m2/repository/com/aegispay
          key: aegispay-libs-${{ github.sha }}

  # ── Step 2: Build + test each changed service ───────────────────────────────
  # Template used by all 10 service jobs below.
  # A service runs if:
  #   (a) its own files changed, OR
  #   (b) shared libs changed (because all services depend on them)

  build-api-gateway:
    name: Build api-gateway
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['api-gateway'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/api-gateway --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-api-gateway, path: services/api-gateway/target/surefire-reports/ }

  build-user-service:
    name: Build user-service
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['user-service'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/user-service --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-user-service, path: services/user-service/target/surefire-reports/ }

  build-transaction-service:
    name: Build transaction-service
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['transaction-service'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/transaction-service --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-transaction-service, path: services/transaction-service/target/surefire-reports/ }

  build-ledger-service:
    name: Build ledger-service
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['ledger-service'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/ledger-service --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-ledger-service, path: services/ledger-service/target/surefire-reports/ }

  build-payment-orchestrator:
    name: Build payment-orchestrator
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['payment-orchestrator'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/payment-orchestrator --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-payment-orchestrator, path: services/payment-orchestrator/target/surefire-reports/ }

  build-risk-engine:
    name: Build risk-engine
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['risk-engine'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/risk-engine --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-risk-engine, path: services/risk-engine/target/surefire-reports/ }

  build-notification-service:
    name: Build notification-service
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['notification-service'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/notification-service --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-notification-service, path: services/notification-service/target/surefire-reports/ }

  build-ai-platform:
    name: Build ai-platform
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['ai-platform'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/ai-platform --also-make -Dspring.profiles.active=test
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY_TEST }}
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-ai-platform, path: services/ai-platform/target/surefire-reports/ }

  build-reconciliation-service:
    name: Build reconciliation-service
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['reconciliation-service'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/reconciliation-service --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-reconciliation-service, path: services/reconciliation-service/target/surefire-reports/ }

  build-data-pipeline:
    name: Build data-pipeline
    runs-on: ubuntu-latest
    needs: [detect-changes, build-libs]
    if: |
      needs.detect-changes.outputs['data-pipeline'] == 'true' ||
      needs.detect-changes.outputs.libs == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - name: Build & test
        run: mvn --batch-mode --no-transfer-progress verify -pl services/data-pipeline --also-make -Dspring.profiles.active=test
      - uses: actions/upload-artifact@v4
        if: always()
        with: { name: test-results-data-pipeline, path: services/data-pipeline/target/surefire-reports/ }

  # ── Step 3: Helm lint ───────────────────────────────────────────────────────
  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.helm == 'true'
    steps:
      - uses: actions/checkout@v4
      - uses: azure/setup-helm@v4
        with: { version: v3.15.0 }
      - name: Lint Helm chart
        run: helm lint infra/helm/aegispay

  # ── Step 4: Docker build & push (prod branch only, push events only) ────────
  # Only runs for services that were built (changed or libs changed).
  # Uses a reusable pattern: each service has its own docker job that only
  # fires if the corresponding build job ran (via 'needs' + 'if' combo).
  #
  # We use `always()` + check the build job result so skipped builds don't
  # block Docker push for services that DID change.

  docker-api-gateway:
    name: Docker — api-gateway
    runs-on: ubuntu-latest
    needs: build-api-gateway
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.build-api-gateway.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/api-gateway --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/api-gateway
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with:
          context: .
          file: services/api-gateway/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  docker-user-service:
    name: Docker — user-service
    runs-on: ubuntu-latest
    needs: build-user-service
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-user-service.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/user-service --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/user-service
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/user-service/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-transaction-service:
    name: Docker — transaction-service
    runs-on: ubuntu-latest
    needs: build-transaction-service
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-transaction-service.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/transaction-service --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/transaction-service
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/transaction-service/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-ledger-service:
    name: Docker — ledger-service
    runs-on: ubuntu-latest
    needs: build-ledger-service
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-ledger-service.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/ledger-service --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/ledger-service
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/ledger-service/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-payment-orchestrator:
    name: Docker — payment-orchestrator
    runs-on: ubuntu-latest
    needs: build-payment-orchestrator
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-payment-orchestrator.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/payment-orchestrator --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/payment-orchestrator
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/payment-orchestrator/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-risk-engine:
    name: Docker — risk-engine
    runs-on: ubuntu-latest
    needs: build-risk-engine
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-risk-engine.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/risk-engine --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/risk-engine
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/risk-engine/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-notification-service:
    name: Docker — notification-service
    runs-on: ubuntu-latest
    needs: build-notification-service
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-notification-service.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/notification-service --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/notification-service
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/notification-service/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-ai-platform:
    name: Docker — ai-platform
    runs-on: ubuntu-latest
    needs: build-ai-platform
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-ai-platform.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/ai-platform --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/ai-platform
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/ai-platform/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-reconciliation-service:
    name: Docker — reconciliation-service
    runs-on: ubuntu-latest
    needs: build-reconciliation-service
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-reconciliation-service.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/reconciliation-service --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/reconciliation-service
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/reconciliation-service/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  docker-data-pipeline:
    name: Docker — data-pipeline
    runs-on: ubuntu-latest
    needs: build-data-pipeline
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.build-data-pipeline.result == 'success'
    permissions: { contents: read, packages: write }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { java-version: "${{ env.JAVA_VERSION }}", distribution: temurin, cache: maven }
      - uses: actions/cache/restore@v4
        with: { path: ~/.m2/repository/com/aegispay, key: "aegispay-libs-${{ github.sha }}" }
      - run: mvn --batch-mode --no-transfer-progress package -pl services/data-pipeline --also-make -DskipTests
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with: { registry: "${{ env.REGISTRY }}", username: "${{ github.actor }}", password: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.IMAGE_PREFIX }}/data-pipeline
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=latest
      - uses: docker/build-push-action@v5
        with: { context: ., file: services/data-pipeline/Dockerfile, push: true, tags: "${{ steps.meta.outputs.tags }}", cache-from: "type=gha", cache-to: "type=gha,mode=max" }

  # ── Step 5: Patch Helm values & trigger ArgoCD (prod branch only) ───────────
  update-helm-tags:
    name: Update Helm Image Tags
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs:
      - docker-api-gateway
      - docker-user-service
      - docker-transaction-service
      - docker-ledger-service
      - docker-payment-orchestrator
      - docker-risk-engine
      - docker-notification-service
      - docker-ai-platform
      - docker-reconciliation-service
      - docker-data-pipeline
    # Run even if some docker jobs were skipped (service didn't change)
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      (
        needs.docker-api-gateway.result == 'success' ||
        needs.docker-user-service.result == 'success' ||
        needs.docker-transaction-service.result == 'success' ||
        needs.docker-ledger-service.result == 'success' ||
        needs.docker-payment-orchestrator.result == 'success' ||
        needs.docker-risk-engine.result == 'success' ||
        needs.docker-notification-service.result == 'success' ||
        needs.docker-ai-platform.result == 'success' ||
        needs.docker-reconciliation-service.result == 'success' ||
        needs.docker-data-pipeline.result == 'success'
      )
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install yq
        uses: mikefarah/yq@v4.44.2

      - name: Patch image tags for changed services
        env:
          SHA: ${{ github.sha }}
        run: |
          # Only patch services whose docker job succeeded
          patch_tag() {
            local svc="$1"
            local key="$2"
            yq e ".services.${key}.image.tag = \"${SHA}\"" -i infra/helm/aegispay/values-prod.yaml
            echo "Patched ${svc} → ${SHA}"
          }
          [[ "${{ needs.docker-api-gateway.result }}"          == "success" ]] && patch_tag api-gateway api_gateway
          [[ "${{ needs.docker-user-service.result }}"         == "success" ]] && patch_tag user-service user_service
          [[ "${{ needs.docker-transaction-service.result }}"  == "success" ]] && patch_tag transaction-service transaction_service
          [[ "${{ needs.docker-ledger-service.result }}"       == "success" ]] && patch_tag ledger-service ledger_service
          [[ "${{ needs.docker-payment-orchestrator.result }}" == "success" ]] && patch_tag payment-orchestrator payment_orchestrator
          [[ "${{ needs.docker-risk-engine.result }}"          == "success" ]] && patch_tag risk-engine risk_engine
          [[ "${{ needs.docker-notification-service.result }}" == "success" ]] && patch_tag notification-service notification_service
          [[ "${{ needs.docker-ai-platform.result }}"          == "success" ]] && patch_tag ai-platform ai_platform
          [[ "${{ needs.docker-reconciliation-service.result }}" == "success" ]] && patch_tag reconciliation-service reconciliation_service
          [[ "${{ needs.docker-data-pipeline.result }}"        == "success" ]] && patch_tag data-pipeline data_pipeline

      - name: Commit updated image tags
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add infra/helm/aegispay/values-prod.yaml
          git diff --cached --quiet || git commit -m "ci: update image tags to ${{ github.sha }} [skip ci]"
          git push
