<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — Compose handles all visual theming; this just sets window behaviour -->
    <style name="Theme.AegisPay" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/aegis_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
