# AegisPay — Local Development Stack
# Runs all infrastructure dependencies for local development.
#
# Usage:
#   docker compose up -d          # start all infra
#   docker compose down -v        # stop + remove volumes
#   docker compose logs -f kafka  # tail a specific service
#
# After startup, run each Spring Boot service locally (mvn spring-boot:run)
# pointing at the ports below, or run the full backend with:
#   mvn -pl services/user-service spring-boot:run

services:

  # ── PostgreSQL 16 ────────────────────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: aegispay-postgres
    environment:
      POSTGRES_USER: aegispay
      POSTGRES_PASSWORD: aegispay_dev
      POSTGRES_DB: aegispay
    ports:
      - "5433:5432"   # 5433 on host avoids conflict with local PostgreSQL
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/local/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aegispay"]
      interval: 10s
      retries: 5
    restart: unless-stopped

  # ── DB init (runs every start, idempotent — ensures all databases exist) ─────
  db-init:
    image: pgvector/pgvector:pg16
    container_name: aegispay-db-init
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGPASSWORD: aegispay_dev
    command: >
      psql -h postgres -U aegispay -d aegispay
        -c "SELECT 'CREATE DATABASE aegispay_users'::text        WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_users')        \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_transactions'::text WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_transactions') \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_ledger'::text       WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_ledger')       \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_sagas'::text        WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_sagas')        \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_risk'::text         WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_risk')         \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_ai'::text           WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_ai')           \gexec"
        -c "SELECT 'CREATE DATABASE aegispay_keycloak'::text     WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname='aegispay_keycloak')     \gexec"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_users        TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_transactions  TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_ledger        TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_sagas         TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_risk          TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_ai            TO aegispay"
        -c "GRANT ALL PRIVILEGES ON DATABASE aegispay_keycloak      TO aegispay"
    restart: "no"

  # ── Redis 7 ─────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: aegispay-redis
    command: redis-server --requirepass aegispay_dev --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "aegispay_dev", "ping"]
      interval: 10s
      retries: 5
    restart: unless-stopped

  # ── MongoDB 7 ────────────────────────────────────────────────────────────────
  mongodb:
    image: mongo:7
    container_name: aegispay-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: aegispay
      MONGO_INITDB_ROOT_PASSWORD: aegispay_dev
      MONGO_INITDB_DATABASE: aegispay
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: ["CMD-SHELL", "mongosh --eval \"db.adminCommand('ping').ok\" --quiet 2>/dev/null | grep -q 1"]
      interval: 15s
      retries: 10
      start_period: 60s
    restart: unless-stopped

  # ── Kafka (KRaft, single-node) ───────────────────────────────────────────────
  kafka:
    # Official Apache Kafka image (free) — replaces Bitnami which went commercial.
    # Env vars use KAFKA_ prefix directly (no KAFKA_CFG_ wrapper).
    image: apache/kafka:3.7.0
    container_name: aegispay-kafka
    environment:
      # KRaft mode — no ZooKeeper
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093,EXTERNAL://:9094
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,EXTERNAL://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_DEFAULT_REPLICATION_FACTOR: 1
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      # Fixed cluster ID ensures volume data survives container restarts
      CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"
    ports:
      - "9094:9094"   # host access (EXTERNAL listener)
    volumes:
      - kafka_data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9092 || exit 1"]
      interval: 10s
      retries: 15
      start_period: 30s
    restart: unless-stopped

  # ── Kafka UI (optional — remove if not needed) ───────────────────────────────
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: aegispay-kafka-ui
    depends_on:
      kafka:
        condition: service_healthy
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
    ports:
      - "8090:8080"
    restart: unless-stopped

  # ── Keycloak 24 (multi-IdP dev identity provider) ────────────────────────────
  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    container_name: aegispay-keycloak
    command: start-dev --import-realm
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/aegispay_keycloak
      KC_DB_USERNAME: aegispay
      KC_DB_PASSWORD: aegispay_dev
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HTTP_PORT: 8180
    ports:
      - "8180:8180"
    volumes:
      - ./infra/local/keycloak/realm-export.json:/opt/keycloak/data/import/realm-export.json:ro
    depends_on:
      db-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD-SHELL", "grep -q ':1FF4 ' /proc/net/tcp6"]
      interval: 15s
      retries: 30
      start_period: 300s
    restart: unless-stopped

  # ── ClickHouse (OLAP analytics store) ────────────────────────────────────────
  clickhouse:
    image: clickhouse/clickhouse-server:24.4-alpine
    container_name: aegispay-clickhouse
    environment:
      CLICKHOUSE_DB: aegispay_analytics
      CLICKHOUSE_USER: default
      CLICKHOUSE_PASSWORD: ""          # empty for local dev
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    ports:
      - "8123:8123"    # HTTP interface (JDBC)
      - "9000:9000"    # Native protocol
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./infra/clickhouse/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8123/ping | grep -q Ok"]
      interval: 15s
      retries: 15
      start_period: 520s
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    restart: unless-stopped

  # ── Apache Superset (analytics dashboard) ────────────────────────────────────
  superset-db:
    image: postgres:16-alpine
    container_name: aegispay-superset-db
    environment:
      POSTGRES_DB: superset
      POSTGRES_USER: superset
      POSTGRES_PASSWORD: superset
    volumes:
      - superset_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U superset"]
      interval: 10s
      retries: 5
    restart: unless-stopped

  superset:
    image: apache/superset:3.1.3
    container_name: aegispay-superset
    depends_on:
      clickhouse:
        condition: service_healthy
      superset-db:
        condition: service_healthy
    environment:
      SUPERSET_SECRET_KEY: "aegispay-superset-dev-secret-change-in-prod"
      SUPERSET_LOAD_EXAMPLES: "no"
      DATABASE_DB: superset
      DATABASE_HOST: superset-db
      DATABASE_PASSWORD: superset
      DATABASE_USER: superset
      DATABASE_PORT: "5432"
      DATABASE_DIALECT: postgresql
    ports:
      - "8088:8088"
    volumes:
      - ./infra/superset/superset_config.py:/app/pythonpath/superset_config.py:ro
      - superset_home:/app/superset_home
    command: >
      bash -c "
        superset fab create-admin --username admin --firstname Admin --lastname User --email admin@aegispay.io --password admin 2>/dev/null || true &&
        superset db upgrade &&
        superset init &&
        gunicorn --bind 0.0.0.0:8088 --workers 4 --timeout 120 --limit-request-line 0 --limit-request-field_size 0 'superset.app:create_app()'
      "
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:8088/health || exit 1"]
      interval: 30s
      retries: 20
      start_period: 360s
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
  mongodb_data:
  kafka_data:
  clickhouse_data:
  superset_home:
  superset_db_data:

networks:
  default:
    name: aegispay-local
